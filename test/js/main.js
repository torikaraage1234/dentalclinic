$(document).ready(function(){
   $('.slider').slick({
      arrows: false,
      autoplay: true,
      autoplaySpeed: 2000,
      fade: true,
      // centerMode: true,
      // centerPadding: "30px",
      // slidesToShow: 3,
      // speed: 400,
   });
});